[package]
name = "lemanflow"
version = "0.0.1"
edition = "2024.beta"

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/mainnet" }

[addresses]
lemanflow = "0x0"
hackevents = "0x0"
lemanflowcontracts = "0x0"

[dev-dependencies]
# Test dependencies

[dev-addresses]
# Test addresses
